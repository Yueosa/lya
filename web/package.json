{
  "name": "lya-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build && node scripts/strip-mc-font.mjs",
    "build:full": "vue-tsc --noEmit && vite build",
    "typecheck": "vue-tsc --noEmit"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/test-utils": "^2.4.11",
    "happy-dom": "^20.11.1",
    "jsdom": "^30.0.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "vue-tsc": "^3.3.9"
  },
  "dependencies": {
    "dompurify": "^3.4.12",
    "highlight.js": "^11.11.1",
    "marked": "^18.0.7",
    "vue": "^3.5.40"
  }
}
